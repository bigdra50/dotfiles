# 実装ノート (implementation-notes.html)

仕様に基づく実装タスク中は、実装が仕様からどう逸脱・解釈されているかをユーザーに伝えるため、プロジェクトルートに `implementation-notes.html` を作成・更新する（running ノート）。

Markdown ではなく HTML を使うのは、図・色・折りたたみ・インタラクションといった表現力を活かすため。
テキストの箇条書きで済ませず、グラフィカルでインタラクティブな成果物として作る。

## 記録項目

| 項目             | 内容                                               |
| ---------------- | -------------------------------------------------- |
| Design decisions | 仕様が曖昧な箇所で行った選択                       |
| Deviations       | 意図的に仕様から外した箇所と、その理由             |
| Tradeoffs        | 検討した代替案と、最終的に採用したものを選んだ理由 |
| Open questions   | ユーザーに確認・修正してほしい点                   |

## 表現方針

HTML の表現力を使い、状況が視覚的につかめるノートにする。
以下は推奨パレット。内容に応じて取捨選択し、クリエイティブに組み合わせてよい（固定の必須様式ではない）。

- 折りたたみ: `<details>/<summary>` でセクション・根拠・コードを開閉し、俯瞰性を保つ
- ステータス色分け・バッジ: decided / deviation / open question / tradeoff を色とラベルで区別する
- 図: フロー・状態遷移・シーケンス・データフローを図で示す（Mermaid 等を使ってよい）
- 目次・アンカーナビ: セクションジャンプと、追記箇所への移動を用意する
- 更新の可視化: 最終更新時刻や新規追記の強調など、running ノートとして変化が分かる工夫を入れる

ここに挙げていない表現も歓迎する。題材に合うなら自由に発想してよい。

## 技術方針

- 単一の `implementation-notes.html` で完結させる
- 外部 CDN / ライブラリの利用可（Claude Code はオンライン前提。Mermaid・Chart.js・Tailwind 等）
- 装飾は内容を引き立てるために使う。中身（判断・逸脱・トレードオフ・確認事項）が主

## ルール

- ファイル名は `implementation-notes.html`（固定、HTML 形式）
- 配置はプロジェクトルート
- 作業の進行に合わせて随時追記・更新する
- 実装完了時にユーザーへファイルの存在を知らせる

## 対象タスク

仕様（SPEC）が明示された実装タスク全般。
単発のバグ修正・タイポ修正・1行変更等の小さい作業は対象外。
