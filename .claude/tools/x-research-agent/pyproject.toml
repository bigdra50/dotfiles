[project]
name = "x-research-agent"
version = "0.1.0"
description = "X(Twitter)からsite:x.com検索で情報収集するエージェント"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anyio>=4.12.1",
    "claude-agent-sdk>=0.1.19",
    "httpx>=0.28.1",
]

[project.scripts]
x-research = "main:main"
