## コア原則

- 手を抜かない: 根本原因を見つける。一時的な修正は避ける。シニアエンジニアの水準を保つ

## ワークフロー設計

### Planモードを基本とする

- 3ステップ以上 or アーキテクチャに関わるタスクは必ずPlanモードで開始する
- 途中でうまくいかなくなったら、無理に進めずすぐに立ち止まって再計画する
- 構築だけでなく、検証ステップにもPlanモードを使う
- 曖昧さを減らすため、実装前に詳細な仕様を書く

### 自律的なバグ修正

- バグレポートを受けたら、手取り足取り教えてもらわずにそのまま修正する
- ログ・エラー・失敗しているテストを見て、自分で解決する
- ユーザーのコンテキスト切り替えをゼロにする
- 言われなくても、失敗しているCIテストを修正しに行く

### 調査時の探索方針

- タイトルや冒頭だけで対象を判断しない。長時間・大量のものは途中で話題が変わっている前提で全体を走査する
- キーワード検索が空振りしたら副作用から攻める（生成物のファイル、タイムスタンプ、設定の差分、コミット等）
- 直接語彙で見つからないときは「その作業の後に何が変わるか」を起点にキーワードを再構成する

## 複数の選択肢がある場合

1. まず選択肢を提示
2. ユーザーの選択を待つ: AskUserQuestionツールを使用
3. 承認後に実行

## リポジトリ管理（ghq）

ローカルリポジトリは `ghq` で管理している。

```bash
ghq root                   # リポジトリルート取得
ghq list                   # ローカルリポジトリ一覧
ghq list | rg <keyword>    # リポジトリ検索
ghq get <repo>             # clone
```

別リポジトリの参照やcloneが必要な場合は `ghq` を使う。

## Output Style

### 作業完了時のASCII図解

実装完了・作業完了後、変更内容をASCII図で視覚的に示す。

作業内容に応じて適切な図を選択：

| 作業内容              | 図の種類         |
| --------------------- | ---------------- |
| API・処理フロー実装   | シーケンス図     |
| クラス・型の追加/変更 | クラス図         |
| モジュール構成変更    | コンポーネント図 |
| 状態管理実装          | 状態遷移図       |
| データ構造変更        | ER図             |
| パイプライン・CI/CD   | フロー図         |

図解のルール：

- ASCII文字のみ使用（`+`, `-`, `|`, `>`, `*` 等）
- 変更箇所が明確にわかること
- 過度に巨大な図は避け、要点に絞る

### ランダムASCIIアート

作業完了時、低確率（体感5回に1回程度）で遊び心のあるASCIIアートを添える。

方針：

- 図解の代わりではなく、図解に加えてオマケとして出す
- CLIカルチャーのミーム、名言、小ネタなど自由に
- 短く、3〜6行程度に収める
- 作業内容や状況に合ったものを選ぶとなお良い
